import * as writr from '../src/index';
import { Config } from '../src/classes/config'
import { expect } from 'chai';
import 'mocha';

describe('Writr', () => {

  it('should return the filePath name', () => {

    //todo init config

    expect(true).to.equal(true);
  });

});